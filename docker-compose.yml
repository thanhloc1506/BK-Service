version: "3.7"

services:
  server:
    image: lv-client
    ports:
      - "5000:5000"
    env_file:
      - ./.env
    #    environment:
    #      - URL_API=mongodb
    stdin_open: true
    tty: true

